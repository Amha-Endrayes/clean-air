A Script to setup the Linux Server
## Description
Include an example bash script that installs and sets up Mosquitto MQTT Broker, Grafana, and InfluxDB on a Linux server:
## NOTE
Please note that this is just an example and may not work as is on your server, depending on your system configuration and dependencies. You might need to adjust the commands and paths accordingly.
